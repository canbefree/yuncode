; 启用 Xdebug 调试功能
xdebug.mode = debug

; 设置调试客户端的主机和端口
; host.docker.internal 用于在 Docker 容器中访问主机
; xdebug.client_host = host.docker.internal
; xdebug.client_port = 9003

; 每次请求时自动启动调试会话
xdebug.start_with_request = yes

; 可选：日志记录，便于排查问题
xdebug.log = /var/log/xdebug.log
xdebug.log_level = 10

; 可选：配置路径映射（根据需要调整）
; xdebug.discover_client_host = true
